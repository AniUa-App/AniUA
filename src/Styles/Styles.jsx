import { StyleSheet } from "react-native";
// Примітка: базовий стиль залишаємо статичним; фактичний фон задаємо динамічно через DefaultScreenWidget з контекстом

const Styles = StyleSheet.create({
  defaultScreenWidget: {
    flex: 1,
  },
});

export default Styles;
